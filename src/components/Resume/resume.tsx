import style from "./resume.module.css";

const Resume: React.FC = () => {
  return (
    <section className={style.container}>
      <h1 className={style.heading}>Resume page</h1>
    </section>
  );
};

export default Resume;
